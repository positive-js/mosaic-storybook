import { storiesOf } from '@storybook/angular';
import { boolean, text } from '@storybook/addon-knobs';


storiesOf('Buttons|Toggle Button', module)
    .add('Toggle', () => ({
        template: `
            <div>
                
            </div>
        `,
        props: {

        }
    }));
